let learning_rate = document.getElementById('learning_rate');
let lambda_param = document.getElementById('lambda_param');
let n_iterations = document.getElementById('n_iterations');
let class1_points_x = [];
let class1_points_y = [];
let class2_points_x = [];
let class2_points_y = [];
let pointsCanvas = document.getElementById('points_canvas');



function main(){
    if(!(class1_points_x.equals([]) || class2_points_x.equals([]))){
        // we should send the data now to the server
    }
}