def naive_bayes_classifier(input_points: list, width: int, height: int) -> list:
    """

    :param input_points: The points in the training data, each point has two coordinates, x and y. In addition, it has
    the id of the class it is in
    :param width: The width of the output window
    :param height: The height of the output window
    :return:A list of ints of the size width * height indicating the classes the algorithm decides for the pixels in
    the output window

    """

    results = []

    return results